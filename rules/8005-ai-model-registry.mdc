---
description: Technical execution of AI inference. Defines .env location, key discovery/rotation logic, and the current model registry. (Level 8: Actions)
globs: ["**/*"]
alwaysApply: true
---

# 🤖 8005: AI Model Registry & Rotation (L8)

## 1. Key Vault Location
All AI keys are centralised at:
`C:\GitHub\T1-vrvish-core\theHive\.env`

**Mandate:** Apps and agents MUST read upward to this central vault. Never create local `.env` files for AI keys.

## 2. Key Discovery & Rotation Logic
The `.env` contains a pool of personal keys (e.g., `_PERSONAL_1` through `_PERSONAL_19`) and a business fallback.

- **Discovery:** Agents must parse the `.env` for the pattern `GEMINI_API_KEY_PERSONAL_[N]`.
- **Rotation Order:** 1. Iterate through all **Personal Keys** (1, 2, 3...) sequentially.
    2. Use the next key if the current one returns a `429 (Quota Exhausted)` error.
    3. **Business Fallback:** Use `GEMINI_API_KEY_BUSINESS` ONLY after all personal keys in the pool have been exhausted.

## 3. Current Model Registry (Seren / Generative Language API)
Use **stable REST model ids** returned by `models.list` with `generateContent` support. Google removes older ids (e.g. `gemini-1.5-pro-latest`) from `generateContent` while `listModels` may still succeed — probes can show 200 even when chat 404s on a retired name.

| Tier | Google (Primary) | Anthropic (Parallel) |
| :--- | :--- | :--- |
| **Tier 2 (Dexterity)** | `gemini-2.5-flash` | `claude-3-haiku-20240307` |
| **Tier 3 (Reasoning)** | `gemini-2.5-pro` | `claude-3-5-sonnet-20241022` |
| **Tier 4 (Heavy Lift)** | *(see ai.google.dev models guide)* | `claude-3-opus-20240229` |

**Latest Update:** 2026-04-01